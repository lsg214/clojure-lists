AI Prompts Used
CS378 Assignment (cs378.clj)

Problem 3 - sumsqmr (map and reduce)
- What is map, how do I use map? Examples of map with symbols.

Problem 4 - stdev
- Standard deviation of a list in Java.
- How do I find the mean of a list in Clojure, subtract the mean,
  square each result, find the variance, and take the square root?

Problem 5 - union / set-difference
- Why is the comma an unresolved symbol?
- How do I fix "too many arguments" in defn?

Problem 6 - binomial
- What is the binomial coefficient formula in the Pascal's triangle
  example?

Problem 7 - maxbt
- I have an unmatched bracket -- how do I fix it?
- tree recursion in Clojure

Problem 8 - vars
- How do I use union to combine the sets of variables from the left
  and right subexpressions?
- How do I use lhs and rhs to get the left and right subexpressions
  of an expression?

Problem 9 - occurs
- How do I recursively call lhs and rhs?
- I have an unresolved symbol -- how do I fix it for "item"?

Problem 10 - myeval
- How do I use op, lhs, and rhs to evaluate the expression tree?
- Do switch cases exist in Clojure? Give an example of using one to
  evaluate an expression tree.
- Syntax error: take a look at this code and tell me what’s the issue

Problem 11 - myevalb
- Is there a lookup function for expressions? How do I use assoc
  to create a new association list with a variable and its value?
- How do I create a recursive lookup function in Clojure?
- What is the difference between calling a function and using
  second/first to get a value?

Problem 12 - tojava
- What is a unary minus in Clojure, and how do I handle unary minus
  in an expression tree?
- How do I write a recursive function that takes an expression tree
  and a precedence number, and decides whether to wrap it in
  parentheses?
- How do I write a precedence function in Clojure that maps
  operators to precedence numbers?
- How do I detect when "-" is unary versus binary?
- How do I convert (sin x) to "Math.sin(x)" in Clojure?
- What are the special cases for the Math class (e.g. expt)?
- How do I add a semicolon to the end of the generated string?
- Why do we need precedence in expression trees, especially inside
  a recursive function?
- How do I assign precedence so that "=" is 1, "+" and "-" are 5,
  and "*" and "/" are 6?
- What is the difference between defining "operator?" and just
  using "operator"?
- What functions do I need for this problem?

